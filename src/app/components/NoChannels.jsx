import * as React from 'react';

function NoChannels() {
    return (
        <div className={'container row started'}>
            <p>No channels found to display. Add your first channel by using the search bar at the top.</p>
        </div>
    )
}

export default NoChannels;
